source 'https://github.com/CocoaPods/Specs.git'

platform :ios, '13.0'
use_frameworks!

target 'Spikes' do
  pod 'ErrorPublisher', :path => 'Modules/ErrorPublisher', :testspecs => ['Tests']
  pod 'Alamofire'

  target 'SpikesTests' do
    pod 'Quick'
    pod 'Nimble'
  end
end
